{
  "name": "aria-orchestrator",
  "version": "0.1.0",
  "description": "Artifact-based design workflow orchestrator for Codex",
  "type": "module",
  "bin": {
    "aria": "./dist/src/bin.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
